on:
    pull_request:
        branches-ignore:
            - main

jobs:
    tsc:
        name: Typescript Compile
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: "install"
              run: yarn install

            - name: "tsc"
              run: yarn tsc
