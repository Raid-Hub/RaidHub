on:
    pull_request:
        branches-ignore:
            - main

jobs:
    tsc:
        name: Formatting Check
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: "install"
              run: yarn install

            - name: "format:check"
              run: yarn format:check
